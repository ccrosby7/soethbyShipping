apply plugin: "war"

bootWar {
    mainClassName = "com.crosby.soethbyshipping.SoethbyShippingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
