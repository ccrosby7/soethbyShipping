version: '2'
services:
  soethbyshipping-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/soethbyShipping/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=soethbyShipping
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
